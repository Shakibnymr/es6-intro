// write an arrow function that will take 3 parameters, will multiply the parameters and will return the result.
const multiply = (a,b,c) => a*b*c;
const output = multiply(2,3,2);
// console.log(output);


// write the following sentences in three lines and print the result: 

const sentences = `I am a web developer.
I love to code.
I love to eat biryani.`;
// console.log(sentences);


// write an arrow function that will take 2 parameters: One parameter will come from you and the other will be a default parameter. add these two parameters and return the result.

const add = (a, b=5) => a + b ;
const result = add(3);
console.log(result);